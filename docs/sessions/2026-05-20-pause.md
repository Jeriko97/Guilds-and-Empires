# Session 2026-05-20 — Point de pause

## État

**ÉTAPE 7 — startProductionSlot : 4/5 livrables complétés**

| Livrable | État |
|---|---|
| `recipes.ts` — extension `producibleBy` + export `RECIPE_TO_INVENTORY_KEY` | ✅ |
| `validators.ts` — ajout `requireValidSlotIndex` | ✅ |
| `src/production/startProductionSlot.ts` — handler complet + types + wrapper | ✅ |
| `src/index.ts` — export `startProductionSlot` décommenté | ✅ |
| `src/__test__/startProductionSlot.test.ts` — 11 tests | ⏳ À faire |

## Ce qui reste au retour

1. Écrire `firebase/functions/src/__test__/startProductionSlot.test.ts`
   — 11 tests selon le spec du ticket ÉTAPE 7
2. Lancer l'émulateur : `firebase emulators:start --only firestore,auth --project demo-guilds-empires`
3. Lancer les tests : `cd firebase/functions && npm run test:emulator`
4. Si 11/11 verts : commit de l'ensemble (recipes, validators, handler, index, tests)

## Spec des 11 tests (rappel)

1. Non-authentifié → `unauthenticated`
2. Rate limit (2 appels < 2s) → `resource-exhausted`
3. Schema version invalide → `failed-precondition`
4. buildingId inexistant → `not-found`
5. slotIndex invalide (-1, 3, "abc", 1.5) → `invalid-argument`
6. recipeId invalide ("dragons") → `invalid-argument`
7. Building incompatible avec recipe → `failed-precondition`
   *(approche : cast buildingType fictif ou reporter en Phase 2 si trop complexe)*
8. Slot occupé → `failed-precondition`
9. Ressources insuffisantes + atomicité (pas de mutation Firestore) → `failed-precondition`
10. Démarrage réussi — logs (pas d'input) → slot updaté, inventory inchangé
11. Démarrage réussi — planks (input = 2 logs) → slot updaté, inventory.logs décrémenté de 2

## État du repo à la pause

Derniers commits :
- `36154ae` — 8 tests resolveLoginState (ÉTAPE 5 complète)
- `fa82389` — TD-006
- `c285e5c` — scaffolding Jest

Fichiers modifiés non commités (à inclure dans le commit de fin ÉTAPE 7) :
- `firebase/functions/src/shared/recipes.ts`
- `firebase/functions/src/shared/validators.ts`
- `firebase/functions/src/login/resolveLoginState.ts` (import RECIPE_TO_INVENTORY_KEY depuis recipes)
- `firebase/functions/src/production/startProductionSlot.ts` (nouveau)
- `firebase/functions/src/index.ts`

## Note Java

L'émulateur requiert Java 21. Le PATH du shell ne le prend pas par défaut — exécuter avant le lancement :

```bash
export JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-21.0.11.10-hotspot"
export PATH="$JAVA_HOME/bin:$PATH"
```
