# Session 2026-06-18 — ÉTAPE 17.B client wiring (17.B-1 + 17.B-2)

## Jalon
Boucle production complète câblée côté client : produce (17.A-3) + collect (17.B). resolveLoginState
reste le chargement initial (récolte offline au login) ; collectProduction = récolte manuelle
(no-op si rien de neuf, ligne 51 early-return).

## 17.B-1 — couche service
- `CollectProductionAsync(buildingId, ct)` → `CollectProductionResult { Building, Inventory, Collected, Discarded }`, miroir de StartProductionSlotAsync.
- `CollectProductionResult.Parse(data, buildingId)` : building (id injecté, D-ETAPE16-004) + inventory + collected/discarded (Dictionary<string,int> via (int)GetLong, robuste aux types numériques wire).
- Extraction `ProductionParsingHelpers` (RequireDict/GetValue/GetString/GetLong + ParseBuilding/ParseSlot/ParseInventory/ParseResourceStack). ProductionResult délègue (−89 lignes). AUCUN second parser ; ParseTimestampMs single-source.
- TD-015 ouverte (copies privées résiduelles dans PlayerStateSnapshot).
- Validé Play : startProductionSlot rejoué OK post-refacto.
- Commits : 1d1f1e9 (refactor), 3af4212 (feat), c5d9d14 (docs TD-015).

## 17.B-2 — bouton « Collecter »
- UN bouton global au bâtiment (collectProduction = tous slots), visible si ≥1 slot actif. Élément UXML persistant ré-activé via SetEnabled(true) dans RenderSlots ; SetAllButtonsEnabled étendu ; souscription clicked équilibrée OnEnable/OnDisable (C9).
- CollectAsync miroir de ProduceSlotAsync ; cible _currentBuilding.Id ; succès → _currentBuilding = result.Building; RenderSlots(); SetStatus(FormatCollectMessage(result), succès).
- Validé Play : tap Collecter → collectProduction OK, « Rien à récolter » (no-op correct : login avait déjà récolté l'offline). 0 erreur.
- Commit : 24467cd (feat Collecter button).

## Décisions
- D-ETAPE17.B loggée (decisions-log §8).

## Prochaine étape
Slice production complète. Prochaine slice client à arbitrer (marché / contrats / chaîne complète). ÉTAPE 17 full closure éventuelle.
