# Session 2026-06-02 — ÉTAPE 17.A-3 ProductionScreen + startProductionSlot

## Jalon
Premier écran gameplay live. Boucle `intention client → startProductionSlot →
état serveur rendu` prouvée en Play. Pattern de navigation ScreenManager établi.

## Livrables
- Couche service : `IProductionService`, `FirebaseProductionService` (patron
  `FirebasePlayerService` : GetHttpsCallable → CallAsync → parse → exceptions C6),
  `ProductionResult`, `ProductionServiceException`.
- `StartProductionSlotAsync(buildingId, slotIndex, recipeId, ct)` → renvoie
  `ProductionResult { BuildingSnapshot Building; InventoryState Inventory }`.
- `ProductionScreen : BaseScreen` (UI Toolkit) ; C3 (aucun SDK Firebase dans l'UI),
  C9 (CTS OnEnable/OnDisable). Liste les slots, bouton « Produire Logs » sur slot idle.
- `ParseTimestampMs` passé `private → internal` (réutilisé par `ProductionResult`,
  source unique préservée — D-ETAPE17.A-2). AUCUN second parser.
- AppBootstrap : `IProductionService` enregistré (bloc firebaseReady) ; step 7 =
  `ScreenManager.SetRoot(_productionScreen)` remplace l'activation ad-hoc DebugScreen
  (DebugScreen reste en scène, inactif).
- Boot.unity : GO ProductionScreen inactif au départ, même PanelSettings, wiré + committé.

## Tâche 0 (vérifs read-only)
- Réponse CF `building` = `BuildingDocument` SANS id → service injecte le buildingId
  (D-ETAPE16-004), jamais reconstruit par convention.
- Transaction = 2 écritures (building.slots + player.inventory), aucune autre collection
  → rendu direct `{building, inventory}` suffisant, zéro re-`resolveLoginState`.

## Commits
- ff6575c — feat(client): add IProductionService and startProductionSlot wrapper
- d1c7590 — feat(client): add ProductionScreen wired via ScreenManager.SetRoot
- f7ba06c — docs: log D-ETAPE17.A-3 and reconcile startProductionSlot signature

## Validation Play (founder)
Boot : ProductionScreen affiché, DebugScreen inactif, 3 slots idle, UN SEUL
resolveLoginState OK. Clic Slot 0 → « Logs en cours », slots 1&2 cliquables,
startProductionSlot OK, aucun re-resolveLoginState au clic. 0 erreur console.
Bonus : état production persistant au cycle pause/resume → server-side confirmé.

## Décision structurante
Les handlers métier renvoient l'état mis à jour (`{building, inventory}`).
`resolveLoginState` devient le chargement initial uniquement. À réutiliser en 17.B.

## Prochaine étape
TD-014 (corriger test resolveLoginState sleep 6s) PUIS 17.B collectProduction.
